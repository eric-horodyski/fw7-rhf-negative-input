import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw7-rhf-negative-input',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
